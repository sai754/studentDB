create database student;
use student;
create table stu(
    rno integer,
    sname varchar(255),
    sage varchar(255),
    sdept varchar(255),
    stotal varchar(255)
);

insert into stu values (1,'A','16','CS','400'), (2,'B','17','CS','450');
